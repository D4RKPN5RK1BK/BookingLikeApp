@model Number
@{ 
	ViewData["Title"] = "Бронирования";
}
<div class="mt-3 card">
	<iframe width="100%" height="700px" frameborder="0" src="@Url.Action("Table", "Number", new { Model.Id })"></iframe>

</div>

