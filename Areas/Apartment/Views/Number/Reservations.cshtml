@model Number
@{ 
	ViewData["Title"] = "Бронирования";
}
<div class="mt-3 card">
	<iframe frameborder="0" src="@Url.Action("Table", "Number", new { Model.Id })" class="w-100" style="height: 70vh"></iframe>

</div>

