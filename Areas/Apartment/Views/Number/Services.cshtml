@model Number
@{
	ViewData["Title"] = "Сервисы";
}

<div class="card mt-3">
	<div class="card-body">
		<div class="col-sm-8">
			<div id="serviceList">
				<div class="row">
					<div class="form-group col-sm-6">
						<label>Название</label>
						<input type="text" name="name" class="form-control" />
						<span class="alert-danger"></span>
						<input type="checkbox" name="free" />
						<label>Платная услуга</label>
						<button type="button" name="deleteService" class="btn btn-danger btn-sm btn-block">Удалить</button>
					</div>
					<div class="form-group col-sm-6">
						<label>Стоимость</label>
						<input type="text" name="name" class="form-control" />
						<span class="alert-danger"></span>
					</div>
				</div>

				<div class="row">
					<div class="form-group col-sm-6">
						<label>Название</label>
						<input type="text" name="name" class="form-control" />
						<span class="alert-danger"></span>
						<input type="checkbox" name="free" />
						<label>Платная услуга</label>
						<button type="button" name="deleteService" class="btn btn-danger btn-sm btn-block">Удалить</button>
					</div>
					<div class="form-group col-sm-6">
						<label>Стоимость</label>
						<input type="text" name="name" class="form-control" />
						<span class="alert-danger"></span>
					</div>
				</div>

			</div>
			<div>
				<button type="button" class="btn btn-success btn-block mt-5">Добавить сервис</button>
			</div>
		</div>
		
	</div>
</div>

@section Scripts {
	<script>
		$(document).ready(function () {
			$("body").on("click", "button[name='createService']", function () {

			});

			$("body").on("click", "button[name='deleteService']", function () {

			});

			$("body").on("change", "input[name='deleteService']", function () {

			});
		});
	</script>
}
